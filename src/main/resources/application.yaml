server:
  port: 8084
spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=sujan_test;encrypt=true;trustServerCertificate=true;
    username: sa
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false
