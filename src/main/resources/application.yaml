server:
  port: 8084
spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=project;encrypt=true;trustServerCertificate=true;
    username: sa
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false
